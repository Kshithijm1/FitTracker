services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fittrack
      POSTGRES_PASSWORD: fittrack
      POSTGRES_DB: fittrack
    ports:
      - "5433:5432"
    volumes:
      - fittrack-postgres-data:/var/lib/postgresql/data

volumes:
  fittrack-postgres-data:
